# Спортивные челленджи — Telegram-бот + Web App

Telegram-бот с Web App для трекинга ежедневных спортивных челленджей. Первый челлендж —
**«Планка каждый день»**: участники ежедневно присылают видео-кружок с планкой в общий чат,
бот отслеживает выполнение, серии, болезни, штрафы и общий банк, а каждое утро шлёт отчёт
за предыдущий день с мотивационной речью.

## Возможности

- **Подтверждение планки** — бот ловит видео-кружки в общем чате, засчитывает при длительности
  ≥ минимума (по умолчанию 60 сек) и до дедлайна (23:59).
- **Модерация** — под каждым засчитанным кружком админ может нажать «Фейк (×2)» или
  «Не засчитывать»; штрафы пересчитываются автоматически.
- **Болезни** — команда `/sick` или кнопка в боте до 14:00 освобождает от штрафа за день.
- **Заморозка серии** — за каждые 30 дней подряд участник получает заморозку (максимум 3 на руках,
  оба числа меняются в админке). Тратит её сам в кабинете и только на пропущенный день не раньше
  дня, когда она заработана: серия не рвётся, штраф за пропуск остаётся.
- **Дневной отчёт** — каждое утро в чат: кто сделал/пропустил/болел, день челленджа, серии,
  банк и одна из ~40 редактируемых мотивационных речей.
- **Напоминания** — вечером тем, кто ещё не прислал кружок. Каждый участник может задать
  своё время (шаг 30 минут) или отключить личные напоминания в кабинете.
- **Вес и состав тела** — на вкладке «Тело» фитнес-челленджа: история веса, динамика за
  неделю и месяц, графики веса, жира и мышц, обхваты. Данные приходят с телефона вместе с
  тренировками (умные весы пишут их в Apple Health или Health Connect) либо вносятся вручную.
  О свежем замере бот пишет в ЛС, цифры личные: другие видят только имя.
- **Банк** — учётный реестр (штрафы пополняют, траты списывают); сумму можно править вручную.
- **Web App** — профиль (серия, рекорд, пропуски, штрафы, статус), заморозки серии (сколько
  доступно/использовано + история), личные уведомления, рейтинг, правила.
- **Админка** (внутри Web App, для пользователей с правами) — редактирование всех настроек:
  штраф, множитель за фейк, дедлайны, минимум планки, параметры заморозки серии, текст правил,
  даты, чат, банк, речи, участники, лента последних событий участников с ручными правками
  статусов, запуск отчёта.

## Стек

- **Сервер:** Node.js + TypeScript, [grammY](https://grammy.dev) (бот), [Fastify](https://fastify.dev) (API), [Prisma](https://www.prisma.io) + PostgreSQL, node-cron.
- **Фронт:** Vue 3 + Vite (Telegram Web App).
- **Деплой:** Docker Compose (app + Postgres + Caddy с авто-HTTPS).

## Структура

```
challenge/
├── server/                 # бот + API + cron
│   ├── src/
│   │   ├── bot/            # grammY: бот, клавиатуры, хэндлеры
│   │   ├── api/            # Fastify: сервер, auth(initData), роуты
│   │   ├── jobs/           # планировщик (отчёт, напоминания)
│   │   ├── services/       # доменная логика (статусы, серии, банк, отчёт…)
│   │   ├── lib/            # config, prisma, time, валидация initData
│   │   └── index.ts        # точка входа
│   └── prisma/             # schema.prisma + seed.ts
├── web/                    # Vue 3 Web App
│   └── src/
│       ├── components/     # Profile / Leaderboard / Rules / Admin
│       ├── api.ts, types.ts, telegram.ts, helpers.ts, styles.css
│       └── App.vue, main.ts
├── Dockerfile              # multi-stage: web build → server build → runtime
├── docker-compose.yml
├── Caddyfile
└── .env.example
```

## Настройка бота в Telegram (@BotFather)

1. Создайте бота: `/newbot`, получите **BOT_TOKEN**.
2. **Отключите privacy mode** (иначе бот не увидит чужие кружки в группе):
   `/setprivacy` → выберите бота → **Disable**.
   *Либо* сделайте бота **администратором** общей группы — тогда он получает все сообщения
   независимо от privacy mode.
3. Привяжите Web App: `/newapp` (или `/setmenubutton`) и укажите URL `https://<ваш-домен>`.
4. Узнайте свой Telegram ID (напишите [@userinfobot](https://t.me/userinfobot)) и впишите его
   в `ADMIN_TELEGRAM_IDS`.

## Деплой на VPS (Docker)

Требуется: домен, указывающий A-записью на IP сервера, и установленный Docker + Docker Compose.

```bash
cp .env.example .env
# заполните BOT_TOKEN, PUBLIC_DOMAIN, WEBAPP_URL, WEBHOOK_SECRET, ADMIN_TELEGRAM_IDS, POSTGRES_PASSWORD
docker compose up -d --build
```

При старте контейнер `app`:
- применяет схему к БД (`prisma db push`),
- сидирует челлендж «Планка» и ~40 речей (идемпотентно),
- запускает сервер (бот + API + cron) и устанавливает webhook.

Caddy сам получит TLS-сертификат Let's Encrypt для `PUBLIC_DOMAIN`.

После запуска:
1. Добавьте бота в общую группу (админом либо с отключённым privacy mode).
2. В группе выполните `/bindchat` (от админа) — чат привяжется к челленджу, кружки начнут
   засчитываться. Узнать id чата можно командой `/chatid`.

   В группе с темами команду пишут в том топике, куда должны приходить отчёты и напоминания:
   бот его запомнит. Кружки засчитываются из любого топика чата. У фитнес-челленджа чат и топик
   задаются в админке (`/chatid` в нужном топике покажет оба id). Пропадёт или закроется топик —
   бот напишет в общий чат, в логе будет строка `[chat]`.
3. Откройте Web App у бота и нажмите «Участвовать» в карточке челленджа — участие явное,
   само оно не появляется. Там же профиль и (для админов) раздел «Админ».

## Локальная разработка

Нужны Node.js 20+ и PostgreSQL (можно поднять только БД: `docker compose up -d db`).

```bash
# сервер
cd server
cp ../.env.example .env        # DATABASE_URL → ваш локальный/докерный Postgres
#   для localhost: postgresql://challenge:challenge@localhost:5432/challenge
#   USE_WEBHOOK=false (long polling, не нужен публичный URL для самого бота)
npm install
npm run prisma:generate
npm run prisma:migrate:dev      # или: npx prisma db push
npm run prisma:seed
npm run dev                     # бот (long polling) + API на :3000

# фронт (в другом терминале)
cd web
npm install
npm run dev                     # Vite на :5173, /api проксируется на :3000
```

Для проверки Web App в браузере без Telegram задайте `web/.env`:
```
VITE_DEV_TELEGRAM_ID=<ваш_telegram_id>
```
— сервер в dev-режиме (`NODE_ENV != production`) примет этот заголовок вместо initData.

Сам Web App внутри Telegram требует HTTPS — для тестирования поднимите туннель
(`cloudflared tunnel --url http://localhost:5173` или `ngrok http 5173`) и укажите его URL в
`WEBAPP_URL` и в кнопке Web App у @BotFather.

### Быстрый smoke-тест API (без бота)

Проверяет весь путь API → сервисы → БД на запущенном Postgres с применённой схемой и сидом:

```bash
cd server
# поднимите БД (docker compose up -d db) и примените схему: npx prisma db push && npm run prisma:seed
DATABASE_URL=postgresql://challenge:challenge@localhost:5432/challenge \
  ADMIN_TELEGRAM_IDS=999 NODE_ENV=development npm run smoke
```

Скрипт дёргает `/api/challenge`, `/api/me`, `/api/leaderboard`, админские роуты (банк, речи,
участники, ручная правка дня, запуск отчёта) под dev-пользователем `999` и печатает результаты.

Пересчёт заморозок серии по всем участникам (сколько заработано / потрачено / доступно):

```bash
cd server
DATABASE_URL=postgresql://challenge:challenge@localhost:5432/challenge npm run freezes
```

## Типы челленджей

У челленджа есть `kind`:

| kind | Что это | Механика |
|---|---|---|
| `plank` | «Планка каждый день» | кружки, дедлайны, больничные, серии, заморозки, штрафы, банк, отчёты |
| `fitness` | челлендж на N дней | недельная норма тренировок, жизни, личные цели и замеры — см. ниже |

Роуты спрашивают не тип, а возможность челленджа: карта `CAPABILITIES` в
`server/src/services/challenge.ts` перечисляет, что умеет каждый `kind`, и неизвестный тип не
умеет ничего. Планочные роуты (`/me`, `/leaderboard`, `/sick`, `/freezes`, `/notifications`)
требуют `dailyCheckin` и остальным отвечают `400 not_applicable` — так новая механика не
наследует молча чужие больничные и штрафы.

Планка одна: бот, её планировщик и её админка живут вокруг `getActiveChallenge()`, который
возвращает только `kind = 'plank'`. Фитнес-челленджей может быть несколько, поэтому у них
свои роуты по id (`/api/admin/challenges/:id/…`) и свой блок планировщика.

Участие в любом челлендже — явное. Ни `/start`, ни открытие приложения в челлендж не
записывают: человек видит его в списке под заголовком «Ещё доступно» и жмёт «Участвовать»
(`POST /api/challenges/:id/join`). Так зашедший ради тренировок не оказывается в планке со
штрафами за пропуски. Из фитнес-челленджа можно выйти там же (`/leave`) — история веса и
тренировок остаётся; из планки участника убирает админ.

Планка заводится сидом (`key: planka`). Фитнес-челлендж создаёт админ: «Админ» → «Фитнес» →
«Новый челлендж».

## Фитнес-челлендж

Челлендж на заданное число дней (`durationDays`; пусто — бессрочный). У каждого своя цель,
общее для всех — дисциплина: обязательные тренировки каждую неделю.

- **Точка отсчёта и цель.** При вступлении участник сначала фиксирует стартовые замеры, а уже
  от них выбирает цель (похудеть, снизить % жира, набрать мышцы либо свою). Прогресс — в процентах
  от старта до цели; «текущее значение» — среднее до трёх замеров за неделю, потому что весы шумят.
  Вес вносится вручную или приходит с умных весов, обхваты — вручную.
- **Мышцы — в килограммах или процентах**, на выбор участника. Хранилище здоровья отдаёт долю, и в базе
  лежит именно она; килограммы выводятся из доли и веса того же замера. Цель запоминает свою
  единицу: «37 кг мышц» в проценты не пересчитать, не угадав будущий вес.
- **Неделя** — 7 дней от даты старта, а не календарная. На неполную неделю (хвост челленджа,
  вступление посреди недели) норма пропорционально меньше, с округлением вверх.
- **Зачёт.** В норму идёт любая активность от `minWorkoutMin` минут, не больше
  `maxWorkoutsPerDay` в день. Остальные тренировки остаются в журнале и дают калории.
- **Жизни.** Итог недели подводится на следующий день после её конца в `weekCloseTime` (зазор
  на досинхронизацию с часов). Не набрал норму — минус жизнь, жизни кончились — выбыл:
  остаётся в таблице и может вести данные, но вне зачёта. Жизни нигде не хранятся, а выводятся
  прогоном итогов недель, поэтому прощение недели админом само возвращает участника в игру.
- **Задним числом итог не меняется.** Ручная тренировка, внесённая в уже закрытую неделю,
  остаётся в журнале, но снимок недели не трогает. Исключения: админ нажал «Пересчитать», либо
  тренировка досинхронизировалась с устройства и закрыла норму — тогда жизнь возвращается сама.
- **Видимость.** Всем видны жизни, тренировки и процент к цели. Вес, % жира, обхваты —
  только самому участнику; цифры он может открыть галочкой в разделе «Ещё».
- **Админ** может простить неделю (болезнь, отпуск), пересчитать её, вернуть выбывшего и снять
  тренировку с зачёта.

Одна тренировка из двух источников считается один раз: записи, перекрывающиеся по времени
больше чем наполовину, — дубли, и в зачёт идёт запись из более точного источника (браслет,
затем телефон-хаб, затем ручная). Удалённая тренировка остаётся тумбстоуном, чтобы повторная
синхронизация её не вернула.

### WHOOP

Тренировки с браслета приходят сами: вебхук сообщает о новой тренировке, сервер забирает её из
API WHOOP; раз в полчаса идёт сверка за последние двое суток — на случай потерянного вебхука.

1. Создайте приложение на [developer.whoop.com](https://developer.whoop.com):
   - Redirect URL: `https://<домен>/api/oauth/whoop/callback`
   - Webhook URL: `https://<домен>/api/webhooks/whoop` (версия v2)
   - Scopes: `offline`, `read:workout`, `read:profile`
2. Впишите в `.env` `WHOOP_CLIENT_ID`, `WHOOP_CLIENT_SECRET` и `TOKEN_ENC_KEY` (любая длинная
   случайная строка; ею шифруются OAuth-токены в базе, менять её потом нельзя) и перезапустите.
3. Участник подключает браслет сам: челлендж → «Ещё» → «Подключения». Согласие даётся во
   внешнем браузере, история с начала челленджа подтягивается в фоне.

Без этих переменных интеграция выключена и в приложении не показывается. Неодобренное
приложение WHOOP ограничено 10 пользователями — для компании друзей достаточно.

### Часы и браслеты через телефон

Почти все устройства пишут тренировки в общее хранилище телефона — Apple Health или Health
Connect. Приложение-экспортёр читает его и шлёт тренировки на личный адрес участника. Настроек
на сервере не нужно (кроме `WEBAPP_URL`): участник сам открывает челлендж → «Ещё» →
«Подключения», получает адрес и токен и вставляет их в приложение на телефоне.

| Телефон | Приложение | Адрес | Что покрывает |
|---|---|---|---|
| iPhone | Health Auto Export (автоматизации — платная функция) | `POST /api/ingest/health-auto-export` | Apple Watch, Garmin, Polar, Suunto |
| Android | Health Connect Webhook (в Google Play платно; бесплатная сборка — в релизах на GitHub) | `POST /api/ingest/health-connect` | Mi Fitness, Zepp (Amazfit), Samsung Health, Garmin, Polar |

Health Connect Webhook — приложение с открытым кодом (AGPL). В Google Play оно платное, но автор
выкладывает бесплатную сборку `app-foss-release.apk` в
[релизах на GitHub](https://github.com/mcnaveen/health-connect-webhook/releases/latest) — та же
программа, ставится файлом; обновлять её удобно через Obtainium. Кнопка на неё есть в инструкции
в приложении. У того же автора есть версия для iPhone («Health Webhook») с тем же форматом —
её примет тот же адрес `health-connect`, — но отправка в ней тоже платная.

Токен передаётся как у весов: `Authorization: Bearer <токен>`, `X-Ingest-Token` или `?token=`.
Он привязан к своему приёмнику — токен от Android к адресу iOS не подойдёт.

Оба приложения шлют скользящее окно, поэтому одна тренировка приходит много раз: сохранение
идемпотентно, а удалённая участником тренировка не возвращается. У Health Connect в тренировке
нет ни id, ни калорий — она опознаётся по моменту начала, а калории и пульс собираются из
соседних массивов по пересечению во времени. Лимит тела запроса для этих адресов поднят до
25 МБ: Health Auto Export кладёт в тренировку пульс по секундам и маршрут.

Huawei Health в Health Connect напрямую не пишет — таким участникам остаётся ручной ввод.

### Правки и удаления у источника

Поправленная тренировка приезжает сама: `externalId` у неё тот же, и выгрузка её обновляет.
А вот об удалении приложения-хабы молчат — стёр её человек в Mi Fitness или в WHOOP, а у нас
она осталась бы висеть. Поэтому отправитель может сказать об этом сам, добавив к обычной
выгрузке одно из двух полей (у HAE — внутри `data`, у Health Connect — в корне):

```json
{ "data": { "workouts": [ … ], "deleted": ["id-1", "id-2"] } }
{ "data": { "workouts": [ … ], "window": { "from": "2026-09-23T00:00:00Z", "to": "2026-09-23T23:59:59Z" } } }
```

`deleted` — список `externalId`, которых у источника больше нет. `window` — ручательство, что
`workouts` это **всё**, что у отправителя есть за интервал: тренировки того же источника внутри
интервала, которых в выгрузке не было, помечаются удалёнными. Так одна выгрузка приводит нашу
картину к состоянию источника, включая удаления, о которых иначе никто бы не узнал.

Удаление — тумбстоун, как и у ручного: следующая выгрузка тренировку не воскресит. Окно работает
только когда указано явно, поэтому скользящее окно Health Auto Export и Health Connect (они этих
полей не шлют) по-прежнему ничего не сносит. Сколько записей помечено, видно в ответе (`deleted`)
и в журнале приёма (`workoutsDeleted`).

### Вес и состав тела через Health Connect

Тот же вебхук `health-connect` принимает и взвешивания: в приложении Health Connect Webhook
достаточно разрешить читать Weight, Body fat, Lean body mass и Body water — и всё, что пишут
в Health Connect весы с приложением (Mi Fitness, Zepp, Samsung Health, Withings), приходит вместе
с тренировками. Отдельный токен для весов при этом не нужен.

В выгрузке это отдельные массивы точек, каждая со своим временем:

```json
{ "weight": [ { "kilograms": 84.3, "time": "2026-09-22T05:31:12Z" } ],
  "body_fat": [ { "percentage": 24.1, "time": "2026-09-22T05:31:12Z" } ],
  "lean_body_mass": [ { "kilograms": 63.9, "time": "2026-09-22T05:31:12Z" } ],
  "body_water_mass": [ { "kilograms": 46.2, "time": "2026-09-22T05:31:12Z" } ] }
```

Точки одного взвешивания склеиваются по окну в минуту (весы иногда пишут их с разбегом в
секунды). Без веса точка не сохраняется. Мышцы и вода в базе лежат долей
от веса, поэтому килограммы из Health Connect пересчитываются через вес того же взвешивания.
Сохранение идемпотентно по времени замера; если жир приехал отдельной выгрузкой позже веса,
он дописывается к существующей записи, а не затирает её. О свежем одиночном замере бот пишет
в ЛС; о пачке (первая выгрузка истории) молчит.
Ответ вебхука получает блок `body: { received, created, updated }`.

### Вес и состав тела через Apple Health

Вебхук `health-auto-export` принимает взвешивания из метрик Apple Health: в автоматизации
Health Auto Export кроме Workouts нужно добавить Health Metrics и отметить Weight & Body Mass,
Body Fat Percentage и Lean Body Mass. Тогда всё, что пишут в «Здоровье» весы с приложением
(Withings, Garmin Index, Renpho, Zepp Life), приходит вместе с тренировками — второй токен и
отдельный токен для весов при этом не нужен.

В выгрузке это метрики со своими единицами и точками:

```json
{ "data": { "metrics": [
  { "name": "weight_body_mass",    "units": "kg", "data": [ { "qty": 84.3, "date": "2026-09-22 08:31:12 +0300" } ] },
  { "name": "body_fat_percentage", "units": "%",  "data": [ { "qty": 24.1, "date": "2026-09-22 08:31:12 +0300" } ] },
  { "name": "lean_body_mass",      "units": "kg", "data": [ { "qty": 63.9, "date": "2026-09-22 08:31:12 +0300" } ] }
] } }
```

Склейка точек по минутному окну, требование веса и пересчёт мышц в долю — такие же, как у
Health Connect. Отличий три. Единицы зависят от настроек телефона, поэтому фунты и стоуны
переводятся в килограммы. Долю жира HealthKit хранит частью единицы, а приложение в разных
версиях отдаёт то `0.241`, то `24.1` — значение не больше единицы считается долей. Процента
воды в HealthKit нет вовсе, у iPhone эта графа остаётся пустой.

### Журнал выгрузок

Логи контейнера живут только до следующего деплоя, поэтому каждое обращение телефона к приёмнику
пишется в таблицу `IngestLog`: когда, какой хаб, успех или нераспознанное тело, размер запроса,
сколько тренировок и взвешиваний приехало и сколько из них сохранилось. Храним последние 10
выгрузок по каждому хабу участника, старые вытесняются новыми.

Отдельно пишется сводка тела запроса — имена метрик и длины массивов, без самих данных:

```json
{ "workouts": 2, "metricsTotal": 2, "metrics": { "weight_body_mass": 1, "step_count": 1 } }
{ "arrays": { "exercise": 3, "weight": 1, "body_fat": 0 } }
```

По ней видно то, чего не видно по счётчикам: включены ли в приложении на телефоне нужные
галочки. Если взвешиваний всегда ноль и метрик веса в сводке нет, дело не в сервере — человек
не отметил их в автоматизации.

Смотреть журнал: «Админ» → «Фитнес» → участник → **«Выгрузки»**. Запрос с неизвестным токеном
в журнал не попадает (непонятно, в чей), такие отказы остаются в логе контейнера.

### Питание и баланс калорий

Вкладка «Еда» — личный дневник: другим участникам он не виден и на жизни не влияет. Записать
приём пищи можно двумя способами:

- **по продукту** — поиск по справочнику и граммовка; калории и БЖУ считаются и фиксируются
  в записи, так что поздние правки справочника историю не меняют;
- **цифрой** — просто число калорий, когда оно известно с упаковки или из меню.

Справочник — общий: ~275 базовых продуктов и блюд (`server/prisma/data/foods.ru.json`, значения
на 100 г) приходят сидом при старте и обновляются, если цифры в файле поправили. Чего нет —
участник добавляет сам как «свой продукт», и его находят остальные. В поиске каждое слово
запроса должно встретиться в названии; готовая еда выше сухой крупы и сырого мяса.

Упакованные продукты (конкретный йогурт, батончик) ищутся в [Open Food Facts](https://ru.openfoodfacts.org)
— открытой базе, которую наполняют пользователи. Запрос уходит только по кнопке «Искать в Open
Food Facts», а не на каждый символ: сервис просит не больше 10 поисков в минуту с адреса и
внятный User-Agent (у нас порог 8 и десятиминутный кэш одинаковых запросов). Найденное оседает
в общем справочнике, так что во второй раз продукт находит уже локальный поиск. Записи без
названия или калорий и явный мусор отбрасываются. Если сервис недоступен, дневник работает
как обычно. Адрес задаётся `OFF_API_BASE` (пустое значение выключает внешний поиск).

Баланс дня: `съедено − (базовый обмен × бытовая активность + тренировки)`. Базовый обмен — по
Миффлину — Сан-Жеору из анкеты (пол, рост, год рождения) и последнего веса; коэффициент
активности по умолчанию 1,2. Тренировки в коэффициент не входят намеренно: их калории
посчитаны по факту. Без анкеты баланс не показывается, а экран подсказывает, чего не хватает.

## Как считается планка

- Кружок (`video_note`) от зарегистрированного участника в привязанном чате с
  `duration ≥ minDurationSec` → засчитан (`counted`). После дедлайна → `late` (= пропуск, штраф).
- Содержание (реально ли планка, не монтаж) автоматически не проверяется — для этого ручная
  модерация админом: «Фейк (×2)» (двойной штраф) или «Не засчитывать».
- Нет ни кружка, ни валидного больничного к концу дня → пропуск, штраф `fineAmount`.
- Серия (`streak`) — последовательные дни с засчитанной планкой; валидный больничный её
  «замораживает» (настраивается `freezeStreakOnSick`).
- Заморозка серии (`StreakFreeze`) — участник тратит её в кабинете на пропущенный день: день
  серию не рвёт и не наращивает (как больничный), штраф за пропуск остаётся. Заработанные
  заморозки нигде не хранятся, а всегда считаются из истории: каждые `freezeEveryDays` дней
  подряд — одна, на руках не больше `maxFreezes` (0 в любом из полей выключает фичу). Поэтому
  у всех участников, включая накопивших серию раньше, заморозки посчитались сами. Посмотреть
  расклад по всем: `npm run freezes` в `server/`. Применить заморозку можно только на день не
  раньше того, за который она получена; ручная правка дня админом (кроме «пропуска») снимает
  заморозку и возвращает её участнику.

## Умные весы

Отдельного приёмника для весов нет: вес и состав тела приходят тем же путём, что и тренировки.
Весы со своим приложением (Mi Fitness, Zepp, Samsung Health, Withings, Garmin, Renpho) пишут
замер в Apple Health или Health Connect, а приложение-хаб на телефоне пересылает его сюда —
см. разделы про вес выше. Настраивается это один раз, вместе с тренировками.

```
весы --BLE--> приложение весов --> Apple Health / Health Connect --> хаб --webhook--> POST /api/ingest/...
```

Состав тела считает само приложение весов по сопротивлению тела, цифры ориентировочные —
смотреть стоит на тренд. Весам без приложения, пишущего в хранилище здоровья, остаётся ручной
ввод на вкладке «Тело»: вес, жир, вода и мышцы, задним числом тоже можно.

## Команды бота

| Команда | Доступ | Назначение |
|---|---|---|
| `/start` | все | регистрация + кнопка Web App |
| `/app` | все | кнопка запуска Web App (работает и в группе) |
| `/sick` | участники | сообщить о болезни на сегодня |
| `/rules` | все | правила челленджа |
| `/id` | все | узнать свой Telegram ID |
| `/chatid` | админ | id текущего чата и топика |
| `/bindchat` | админ | привязать текущий чат (и топик, если вызвана в нём) к челленджу |
| `/pinapp` | админ | опубликовать и закрепить кнопку запуска приложения |
| `/report [YYYY-MM-DD]` | админ | вручную сформировать и отправить отчёт |

## Открытие Web App из группы

Inline-кнопки `web_app` Telegram разрешает только в личке. Чтобы запускать приложение из
группы, используется **Direct Link Mini App**: настройте в @BotFather **Main Mini App** (или
`/newapp` с коротким именем) с URL вашего домена. После этого работают `/app`, `/pinapp` и
кнопки в отчётах. `/pinapp` публикует и закрепляет кнопку запуска вверху группы.

## Авто-деплой

Деплой по пушу в `main` через **GitHub Actions на self-hosted раннере**
(`.github/workflows/deploy.yml`). Раннер развёрнут прямо на сервере, поэтому деплой выполняется
локально (без SSH) и не зависит от GitHub-hosted минут/биллинга. Шаги job:
`git fetch && git reset --hard origin/main && docker compose up -d --build`.

Сервер развёрнут как git-клон в `/opt/challenge`; `.env` лежит рядом (в `.gitignore`) и
`git reset --hard` его не трогает. Раннер: служба
`actions.runner.Fedos16-plank-challenge-bot.challenge-vps` (`/opt/actions-runner`, под root).
